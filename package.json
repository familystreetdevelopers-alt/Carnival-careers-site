{
  "name": "carnival-careers-exact-standalone",
  "private": true,
  "scripts": {
    "build": "node build-standalone.mjs && node distribution-pages.mjs"
  },
  "dependencies": {
    "adm-zip": "^0.5.16"
  }
}
